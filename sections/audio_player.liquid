{% schema %}
{
  "name": "Pay button",
  "settings": [
    {
      "type": "product",
      "id": "selected_product",
      "label": "Select Product"
    }
  ],
  "presets": [
    {
      "name": "Play button",
      "category": "Product"
    }
  ]
}
{% endschema %}

{% if section.settings.selected_product and section.settings.selected_product.metafields.custom.audio %}
<div class="play-button">
    <button class="playPauseButton" id="productPlayBtn"> 
        <i class="fa-solid fa-play"></i>
    </button>
</div>
  document.addEventListener('DOMContentLoaded', function() {
  <script src="{{ 'Omni-audio-player.js' | asset_url }}" defer="defer"></script>
});
{% endif %}


{% javascript %}

{% endjavascript %}

{% style %}


{% endstyle %}


